<%- include('./partials/head'); %>

<h3>THE STATS FOR <%=dataSetName%> </h3>

<table>
    <% if (Object.keys(stats).length > 0){ %>
        <% for (let key in stats) { %>
            <tr>
                <th>
                    <%= key %>
                    <%= stats[key] %>
                </th>
            </tr>
            
        <% } %>
    <% } else { %>
        <h3>your dataSet has no numbers</h3>
    <% } %>
</table>